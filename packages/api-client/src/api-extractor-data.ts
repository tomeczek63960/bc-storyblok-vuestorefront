/**
 * `api-client` for Vue Storefront 2 integration with BigCommerce platform.
 *
 * @remarks
 * The `@vue-storefront/bigcommerce-api` library includes everything needed to fetch data from the Bigcommerce platform.
 * It includes API client configuration, API endpoints and types.
 *
 * @packageDocumentation
 */

export * from './index';
